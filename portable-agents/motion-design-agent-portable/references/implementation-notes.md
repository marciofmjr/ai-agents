# Implementation Notes

- Prefer transform plus opacity.
- Avoid layout thrash and expensive paint effects by default.
- Remove or reduce stagger on small screens when it slows reading.
- Keep loops temporary and purpose-driven.
- Treat smooth-scroll libraries as optional, not default.
- Verify keyboard flow, focus visibility, and CTA readability after every motion change.
